# Shared dependencies (install these first, then pipeline-specific deps)

# Core
torch>=2.1.0
torchaudio>=2.1.0
tqdm>=4.60

# Data
boto3>=1.34
datasets>=2.18.0

# Evaluation — ASR (intelligibility)
omnilingual-asr

# Evaluation — naturalness
# UTMOS is loaded via torch.hub (no pip install needed)

# Evaluation — speaker similarity (voice-cloning models only)
speechbrain>=1.0.0

# Evaluation — FAD
scipy>=1.10.0
numpy>=1.24.0

# Benchmark data
# FLEURS Georgian is loaded via: datasets.load_dataset("google/fleurs", "ka_ge")
